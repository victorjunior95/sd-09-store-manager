const errorMiddlewares = require('./errorMiddlewares');
const validProducts = require('./validProducts');
const validSales = require('./validSales');

module.exports = {
  validProducts,
  errorMiddlewares,
  validSales,
};
